# Node.js server port (if we need to change it)
NODE_PORT=8081

# C++ server port
CPP_PORT=8080

# RSA Key size (for encryption)
RSA_KEY_SIZE=2048

# Docker deployment settings
DOCKER_USER=root
DOCKER_APP=sender_app

# Session timeout (in minutes)
SESSION_TIMEOUT=30

# Render-specific environment setup
RENDER_DEPLOY=true

# Node.js Encryption Secret Key (Optional — for HMAC or JWT in the future)
NODE_SECRET_KEY=supersecretcomplicatedkey

# Database paths (JSON file storage)
USERS_DB=users.json
MESSAGES_DB=messages.json
SESSIONS_DB=sessions.json
